import * as $assert from "assert";
import * as $fs from "fs";
import * as $events from "events";
import * as $zlib from "zlib";
import * as $url from "url";
import * as $util from "util";
import * as $crypto from "crypto";
import * as $tls from "tls";
import * as $http from "http";
import * as $https from "https";
import * as net from "net";
import * as tty from "tty";
import * as dgram from "dgram";
import * as querystring from "querystring";
import * as path from "path";
import * as readline from "readline";
import * as childProcess from "child_process";
import * as cluster from "cluster";
import * as os from "os";
import * as vm from "vm";
/**
 * jf is javascript facilities.
 * jf provides an interface for you to easily find assemblies.
 */
export * from "http";


export module jf {
    export module nodeJS3 {
        export function assert(): (typeof $assert) {
            return require("assert");
        }
        export function fs(): (typeof $fs) {
            return require("fs");
        }
        export var http: typeof $http;
    };

    export class nodeJS {
        static get assert(): (typeof $assert) {
            return require("assert");
        }
        static get fs(): (typeof $fs) {
            return require("fs");
        }
        static get http(): (typeof $http) {
            return require("http");
        }
    };
};
//set module.exports is very important for nodejs. the exports is the interface that is exposed to other files;
module.exports = jf;